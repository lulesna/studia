def translate(text):
    if text.startswith('a' or 'e' or 'i' or 'o' or 'u' or 'xr' or 'yr'):
        return text + "ay"
    if text.startswith('ch' or 'st'):
        return text + "ay"
    if text.startswith('*qu'):
        return
    if text[1] = "y" or :
